# Infrastructure & DevOps

Dépôt `infrastructure-devops` (actuellement vide) destiné aux assets d'infra :
- Conteneurisation des services (Docker/Compose/K8s).
- CI/CD (GitHub Actions, runners, lint/tests).
- Provisioning Keycloak, RabbitMQ, PostgreSQL, migrations BDD.

## Prochaines étapes
- Ajouter un `README` de stack locale (Compose) pour démarrer API + front + moteur.
- Documenter les secrets/variables d'environnement partagées (format `.env.example`).
- Scripts de seed (comptes Keycloak, jeux de données de démo).
